{
  "name": "project-elife-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "json-server db.json --routes routes.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kheireddine mechergui",
  "license": "ISC",
  "dependencies": {
    "faker": "^6.6.6",
    "json-server": "^0.17.0"
  }
}
